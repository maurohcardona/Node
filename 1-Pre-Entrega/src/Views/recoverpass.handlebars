<header>
  <nav class="navbar navbar-expand-lg bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><h1>Bentica</h1></a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="nav navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/register">Register</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>
<div class="row">   
  <div class="col-md-4 offset-md-4">
    <div class="card mt-4">
      <div class="card-header">
        <h3 class="text-center">Recuperacion de contrasena</h3>
      </div>
      <div class="card-body">
        <p>Ingrese su mail y verifique su correo para regenerar su contrasena</p>
        <form action="/passwordrecover" method="post">
          <div class="form-group mb-2">
            <input
              type="email"
              name="correo"
              placeholder="Email"
              class="form-control"
            />
          {{#if message}}
            <span class="alert text-start">{{message}}</span>
          {{/if}}
          <button
            class="btn btn-primary btn-block mt-2 form-control"
            type="submit"
          >Recuperar contrasena</button>
        </form>
      </div>
    </div>
  </div>
</div>